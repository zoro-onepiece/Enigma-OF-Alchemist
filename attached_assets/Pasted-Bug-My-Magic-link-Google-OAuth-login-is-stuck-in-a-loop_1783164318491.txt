Bug: My Magic.link Google OAuth login is stuck in a loop. Clicking "Login to Play" → Google account chooser opens → I select my account → Google redirects back to my app → but instead of loading the 3D game, the app shows the same login menu again. Google's side succeeds; my app fails to complete the session. Fix this properly by diagnosing the real cause, not by guessing.
Architecture context: src/lib/magic.js exports the Magic instance (magic-sdk + @magic-ext/oauth2, Arbitrum Sepolia network) plus helpers loginWithGoogle(), handleOAuthRedirect(), getExistingSession(). src/App.jsx has a mount useEffect that calls handleOAuthRedirect() first, then getExistingSession() as fallback, and stores the returned address in walletAddress state — non-null walletAddress renders the game, null renders MainMenu.
Step 1 — Reproduce with logging: Add temporary console.log lines at the start of the App.jsx mount effect and inside handleOAuthRedirect in magic.js: log window.location.href on load (so we see exactly which query params Google/Magic returned with), log whether the OAuth-return detection matched, and log the result or full error object from magic.oauth2.getRedirectResult(). Run the app, perform a login, and read the console output. Report what you find before fixing.
Step 2 — Fix based on what the logs show. The known causes, in order of likelihood:
(a) getRedirectResult called twice (React StrictMode): it may only run once per redirect; StrictMode double-mounts effects in dev. Fix: in magic.js, cache the handleOAuthRedirect result in a module-level singleton promise so the second effect run reuses the same in-flight promise. Do NOT remove StrictMode from main.jsx.
(b) Return-URL params not detected: if the console shows Google returned with query params my detection doesn't cover (e.g. only ?code=...&state=... or ?magic_oauth_request_id=...), broaden the check in handleOAuthRedirect to match the actual params from the log.
(c) getRedirectResult throws: if the log shows a thrown error, read its message. If it's a state/storage mismatch (OAuth state saved in a different origin/tab), confirm I'm testing on the exact whitelisted origin. Whatever the error, after a failure add a rescue path: check magic.user.isLoggedIn() and if true, recover the address via magic.user.getInfo() instead of returning null.
(d) URL params stripped before processing: check whether anything (router, history.replaceState, a redirect in vite config or index.html) cleans the query string before handleOAuthRedirect reads it. The query params must survive until getRedirectResult has consumed them.
(e) Env key missing at runtime: log whether import.meta.env.VITE_MAGIC_PUBLISHABLE_KEY is defined. If undefined, the Magic instance is broken from the start — tell me to re-add the Secret and restart; do not hardcode any key.
Step 3 — Verify end-to-end: After the fix, do a full login: menu → Google → account select → back to app → 3D game loads with the wallet address (0x…) visible in the HUD top-right. Then refresh the page and confirm the session persists (game loads directly without re-login, via getExistingSession). Then remove the temporary console.log lines but KEEP the existing error logging.
Rules: Do not switch to a popup-based login flow. Do not replace Magic with another auth library. Do not modify Scene.jsx, GameHUD.jsx, MainMenu.jsx, my character code, or the Dev Bypass logic. Do not hardcode secrets. Report the root cause you found and exactly what you changed.