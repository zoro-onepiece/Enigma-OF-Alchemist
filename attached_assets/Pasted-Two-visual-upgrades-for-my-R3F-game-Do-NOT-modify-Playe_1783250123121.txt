Two visual upgrades for my R3F game. Do NOT modify Player.tsx internals, character animation, GameHUD internals, or puzzle logic.
Task 1 — Distant scenery (the horizon is empty): Create src/components/scene/environment/DistantScenery.tsx and mount it in the environment. It should add, far outside the playable island (radius ~120–250 units from center):

A ring of 6–10 low-poly mountain silhouettes (simple cone/pyramid geometry, 2–3 muted blue-grey tones, varied heights 20–60, no shadows — castShadow={false}).
2–3 distant floating islands: reuse an existing island/terrain GLB already in the project, scaled small, placed high and far (different heights/directions), with a very slow bobbing animation (useFrame sine, tiny amplitude). No shadows on these either.
A few extra large soft clouds near the horizon using drei <Clouds>.
Then tune the scene fog so distant objects blend softly into the sky color instead of disappearing abruptly — the far fog distance must be beyond the mountains (e.g. fog far ≈ 300) and the fog color must match the sky. Verify from ground level: looking around 360°, the horizon should always show something (mountains/islands/clouds), never empty sky meeting bare ground.

Task 2 — Replace code-generated trees with GLB tree instances: The procedurally generated trees (the simple green sphere-canopy and pink blob trees rendered in GameEnvironment.tsx — e.g. CherryBlossomTree / GreenLeafTree / QuantumTree style components) look inconsistent. Replace their placements with instanced copies of the artist-made low-poly tree GLB models already in the project (the forest/autumn tree assets). Requirements: extract single-tree meshes and render them via drei <Instances> (or <Clone> if multi-mesh) at the SAME positions the code trees currently occupy, with slight random rotation and scale variety (0.8–1.3); enable castShadow/receiveShadow on these near trees; keep total added draw calls low (instancing, not thousands of meshes). Do NOT load the full pre-scattered forest scene GLB as one blob for this — individual tree meshes only. After replacing, remove/comment the old code-tree components from the render (leave their files in place). If the tree GLBs' scale doesn't match, size them so a tree is roughly 3–5× the character's height.
Verify: 360° horizon filled, fog blends smoothly, new trees look consistent with the temple's style, no FPS drop below current baseline, no console errors. Report what you changed and the final counts (mountains, islands, tree instances).