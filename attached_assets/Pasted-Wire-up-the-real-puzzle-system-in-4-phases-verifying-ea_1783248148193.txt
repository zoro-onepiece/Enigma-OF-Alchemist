Wire up the real puzzle system in 4 phases, verifying each. Do not modify Player.tsx internals, playerKeyboardMap, character animations, GameHUD.tsx internals, App.jsx auth logic, or magic.ts. Extend the existing gameStore.ts only — no new stores, no new packages.
Phase 1 — Single source of truth: In gameStore.ts, add score (init 340) with addScore(amount), derive essences as puzzle.solved.size, and make solvePuzzle(id) also add +100 score. In Scene.tsx, remove the hardcoded useState for score/essences and read both from the store, passing them to GameHUD's existing props (essences capped at 4). Health stays as-is.
Phase 2 — Proximity + E key: In GlowingPuzzle.tsx, add a per-frame distance check against PLAYER_WORLD_POS imported from Player.tsx. Within ~3.5 units of an unsolved puzzle: show the existing "Press E" label (hidden otherwise) and listen for the E key via a window keydown listener inside GlowingPuzzle to trigger activation. Only the nearest in-range puzzle responds. Keep onClick working too.
Phase 3 — Real challenge (rune sequence modal): Revive src/components/ui/PuzzleModal.tsx as the challenge UI, replacing placeholder logic with a rune-sequence memory puzzle: 4 colored rune buttons; on open, a fixed 4-step target sequence flashes once (~600ms apart, deterministic per puzzle id, different for each of the 4 puzzles); the player must click the same order. Full correct → store's solvePuzzle(activeId) → brief "An Essence awakens ✨" success state → close. Wrong click → shake/red flash → replay sequence → unlimited retries. Activation (E/click) sets puzzle.activeId + phase='puzzle' which mounts the modal; closing sets phase='exploring'. Style it to match the fantasy HUD (dark stone/amber/emerald). stopPropagation inside the modal so game keys don't leak — don't touch Player.tsx.
Phase 4 — Solved polish: Solved puzzles: rune ring stops bobbing, glows warm gold, not interactable, no label. Verify full loop: walk to puzzle → label in range → E → modal → watch sequence → click correct order → HUD essence +1 and score +100 → puzzle gold → all 4 → HUD 4/4.
Report changes per phase. After all pass, list (don't delete) the dead files you'd clean up and wait for my approval.