I have a scale and alignment problem in my R3F game. Watch for these exact symptoms: my forest model (/models/low_poly_forest.glb, loaded via the WorldModel component) renders as a tiny diorama sitting on top of a large flat green ground plane. My character walks on the flat green plane but is almost as tall as the forest's trees, and she cannot walk into or onto the forest terrain — she just walks around it on the flat plane. Fix this WITHOUT modifying my character's animation or movement logic code.
Step 1 — Make the forest the real world: In Scene.jsx, increase the WorldModel's targetSize prop from its current value to 250. Run the app and visually compare: standing next to a tree, my character's head should reach roughly the lower branches — trees should be 3–5× taller than her. If trees are still too small, increase targetSize in steps of 50 until the proportion looks right. Report the final value you settled on.
Step 2 — Align the walking surface: My character controller moves the character at a fixed ground height (the flat plane's level, y=0). The forest's walkable ground mesh (the mesh named grnd inside the GLB) sits at a different height, which is why she can't walk "in" the forest. Fix by adjusting the WorldModel's position y-value (negative values sink it) until the forest's grass surface is exactly at the character's foot level, y=0. The forest model also contains a water mesh and a slope mesh below ground level — it is correct for those to end up below y=0.
Step 3 — Remove the placeholder plane: Find the flat green ground plane in my scene (likely a <mesh> with planeGeometry or boxGeometry and a green material, or a drei <Plane>/<Grid>). Once the forest ground is aligned at y=0 and large enough to walk on, delete or comment out this placeholder plane. If my character controller raycasts against that plane specifically for ground detection, instead make the plane invisible (visible={false}) rather than deleting it, so movement still works.
Step 4 — Check movement bounds: If my character movement code clamps position to a boundary (e.g., max x/z limits), widen those limits to match the new forest size so she can reach the whole terrain. Do not otherwise change the movement logic.
Step 5 — Center the spawn: Position the character's starting point on an open grassy area of the forest (not inside a tree or in the water). Adjust only the character's initial position prop, nothing else.
Step 6 — Verify: Run the app and confirm: (a) the forest fills the scene as the actual world, (b) the character walks ON the forest grass with feet touching the ground, (c) she can move around trees and across the terrain freely, (d) shadows and HUD still render. Take these one at a time and tell me the final targetSize and position values you used.
Rules: Do not refactor WorldModel.jsx, GameHUD.jsx, or my character/animation code. Only change prop values in Scene.jsx, the placeholder plane, spawn position, and movement bounds. If ground alignment is impossible because my character controller hard-codes a height, report the exact file and line instead of rewriting the controller.