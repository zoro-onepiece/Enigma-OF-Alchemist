I am building a 3D third-person Web3 puzzle/adventure game called "Enigma of Alchemist" using React, Vite, Tailwind CSS, React Three Fiber, and @react-three/drei. My project already loads an animated character via useGLTF inside Scene.jsx — DO NOT modify, refactor, or rename my character code or its animation logic.
I have uploaded these files to the project root: WorldModel.jsx, Scene.jsx, App.jsx, GameHUD.jsx, Foliage.jsx, low_poly_forest.glb, low_poly_tree_scene_free.glb.
Complete these steps in order:
Step 1 — Dependencies: Install three, @react-three/fiber, and @react-three/drei if any are missing. Also verify Tailwind CSS is configured (tailwind.config.js content includes ./src/**/*.{js,jsx} and index.css has the three @tailwind directives). If Tailwind is missing, set it up.
Step 2 — Compress the 3D models: Create the folder public/models/. Then run these exact shell commands:
npx @gltf-transform/cli optimize low_poly_forest.glb public/models/low_poly_forest.glb --compress draco --texture-compress webp
npx @gltf-transform/cli optimize low_poly_tree_scene_free.glb public/models/trees_optimized.glb --compress draco --texture-compress webp
After both succeed, delete the two original .glb files from the project root to save space. If the optimize command fails, fall back to copying the raw files into public/models/ and tell me it failed.
Step 3 — Place code files: Move WorldModel.jsx, GameHUD.jsx, and Foliage.jsx into src/ exactly as provided — do not change their code.
Step 4 — Merge Scene.jsx: My existing src/Scene.jsx contains my working character component. Merge the uploaded Scene.jsx into it: keep my character exactly where it is, and add around it the ambientLight, the shadow-casting directionalLight, Sky, Environment, Suspense wrapper, and the <WorldModel url="/models/low_poly_forest.glb" targetSize={50} position={[0, 0, 0]} /> line. Keep the trees WorldModel commented out for now. Keep OrbitControls only if I don't already have a camera controller.
Step 5 — Merge App.jsx: Update my src/App.jsx so that: the Canvas is wrapped in a div with className="relative h-screen w-screen overflow-hidden bg-slate-950", the Canvas has the shadows prop and dpr={[1, 1.5]}, and <GameHUD /> renders immediately after the closing </Canvas> tag with the props shown in the uploaded App.jsx (health, score, essences, walletAddress, onConnectWallet).
Step 6 — Run and verify: Start the dev server. Confirm in the preview: sky renders, the forest terrain loads with shadows, my character is visible, and the HUD overlay shows the health bar (top-left), Connect Wallet button (top-right), and inventory slots (bottom-right). If the terrain and character sizes look mismatched, adjust only the targetSize prop on WorldModel — never rescale my character.
Rules: Do not refactor, "improve," or reformat any provided code. Do not touch main.jsx. Do not add extra libraries, state managers, or features I didn't ask for. If any step fails, stop and report the exact error instead of improvising a workaround.