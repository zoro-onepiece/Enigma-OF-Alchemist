Do NOT change any code. This is a read-only analysis task. Give me a complete report on the current state of this project's puzzle system and game state, structured exactly like this:
1. Puzzle files inventory: List every file related to puzzles, interactions, or collectibles (search src/ for anything with puzzle, rune, ingredient, cauldron, interact, collect, or similar in filenames or code). For each file: its path, what it does in 2-3 lines, and whether it's actually imported/used anywhere or is dead code.
2. State management: How is game state currently handled? Is zustand installed and used? List every store file (e.g. src/state/gameStore.js) and what values/actions it contains. If state is instead in component useState, say where (which components hold score, essences, health, inventory, puzzle progress).
3. HUD wiring: What does GameHUD currently receive as props, and where do those values come from (store, App.jsx useState, or hardcoded)? Is there a PuzzleHUD or any hint/toast component, and is it rendered?
4. Interaction system: How does the player currently interact with objects — mesh onClick handlers, proximity/distance checks against character position, keyboard events, or nothing yet? If proximity-based, where does the character's position come from (which file exposes it)?
5. Character integration points: Which files contain the character/controller logic (do not describe their internals in depth — just list files and note if they expose position, or read/write any shared state that puzzles would need).
6. Scene composition: What is currently rendered inside Scene.jsx — list the components in order (world model, foliage, character, puzzle layer, lights, controls) so I can see if a PuzzleLayer or equivalent is already mounted.
7. Recently added but unwired: List any files that exist in the project but are never imported (leftover from previous sessions).
Keep each section factual and concise. Do not suggest improvements, do not refactor, do not install anything — report only.