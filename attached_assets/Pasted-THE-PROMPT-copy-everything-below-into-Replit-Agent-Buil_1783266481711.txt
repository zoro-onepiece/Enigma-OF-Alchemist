THE PROMPT — copy everything below into Replit Agent:

Build the full puzzle mini-game system. My world already has 4 GlowingPuzzle areas (GameEnvironment.tsx), a zustand gameStore (puzzle.activeId, puzzle.solved, phase), and Player.tsx exports PLAYER_WORLD_POS. Do NOT modify Player.tsx internals, playerKeyboardMap, character animations, GameHUD.tsx internals, App auth logic, or magic.ts. Work in phases, verify each.
Phase 1 — Interaction (E key + proximity): In GlowingPuzzle.tsx, add a per-frame distance check against PLAYER_WORLD_POS. Within ~3.5 units of an UNSOLVED puzzle, show a "Press E to interact" label (hidden otherwise) and listen for the E key via a window keydown listener inside the component (do not touch playerKeyboardMap). Only the nearest in-range puzzle responds. Keep click activation working too. Activation sets puzzle.activeId and phase='puzzle' in the store.
Phase 2 — Puzzle modal framework: Create src/components/puzzles/PuzzleModal.tsx (replace/supersede the dead ui/PuzzleModal.tsx): a fantasy-styled DOM overlay (dark stone, amber/emerald accents, matching the HUD) that mounts when phase==='puzzle', shows the mini-game for the active puzzle id, has a close (X) button that sets phase='exploring' without solving, and stopPropagation on key/mouse events inside it so game controls don't leak. Map the 4 puzzle ids to 4 different mini-games (Phases 3a–3d), each in its own file under src/components/puzzles/games/.
Phase 3a — Game 1, Rune Memory (Simon Says): 4 colored rune buttons. A sequence flashes (start length 3, ~600ms steps), player repeats it by clicking; correct → next round with sequence +1; win after 3 rounds. Wrong click → red shake, replay the same sequence, unlimited retries.
Phase 3b — Game 2, Alchemy Match-3: 6×6 grid of 5 ingredient types (emoji or colored gems: 🔮🌿⚗️🔥💧). Click one tile then an adjacent tile to swap; only allow swaps that create a 3+ line match; matched tiles clear, tiles above fall down, new ones spawn at top, cascades count. Win: clear 15+ tiles within 20 moves; lose: out of moves → "The mixture failed" → restart button (fresh board, unlimited attempts). Show moves-left and cleared-count counters. Keep the logic simple and bug-free: no diagonal matches, no special candies.
Phase 3c — Game 3, Potion Mixing: Show a recipe scroll with a riddle-style hint indicating 3 ingredients in order (e.g. "First the blood of ember, then the tear of sky, sealed with whisper of leaf" → Fire, Water, Herb). 6 ingredient buttons displayed; player clicks 3 into the cauldron slots; a Brew button checks: exact ingredients in exact order → win; wrong → smoke animation, slots clear, retry with the same recipe. Fixed recipe (deterministic), unlimited retries.
Phase 3d — Game 4, Sigil Pairs (memory): 12 face-down cards in a 4×3 grid, 6 alchemical symbol pairs (🜁🜂🜃🜄🜚☿). Click to flip two; match stays revealed, mismatch flips back after 800ms. Win: all 6 pairs found within 20 flips; over 20 → board reshuffles, retry. Show flips-used counter.
Phase 4 — Victory + store + NFT hook: On winning any mini-game: show a golden victory overlay inside the modal — "✨ Essence Conjured!" with the puzzle's name, a brief glow/particle CSS effect, and a Continue button that calls the store's solvePuzzle(activeId) and sets phase='exploring'. solvePuzzle must add +100 score, and the HUD essences count = puzzle.solved.size (wire Scene.tsx to read score/essences from the store if not already). The solved GlowingPuzzle in the world turns gold, stops bobbing, and is no longer interactable. NFT integration point for my teammate: create src/web3/onEssenceEarned.ts exporting async function onEssenceEarned(puzzleId: string) — currently it just console.logs "TODO: mint NFT for {puzzleId}" — and call it from solvePuzzle. She will implement minting inside that one function later; nothing else should need to change.
Final verify (full loop, Dev Bypass is fine): walk to each of the 4 areas → label appears in range → E opens the correct distinct mini-game → each game is winnable and losable/retryable as specified → victory overlay → HUD essence increments (up to 4/4) and score +100 each → solved pedestals gold and inert → onEssenceEarned logged with the right puzzle id each time → close (X) mid-game leaves the puzzle unsolved and replayable → no console errors, no FPS drop.
Report per phase what you built, and any file the games needed that I should know about.