Fix these 4 issues in my R3F game "Enigma of Alchemist". My character's animation logic (Player.tsx internals), playerKeyboardMap, and GameHUD.tsx internals must NOT be modified. Work in order, verify each.
Task 1 — Blue sky with clouds: Replace the current night Environment/sky with a bright blue daytime sky: drei <Sky> with sun high and low turbidity, plus drei <Clouds>/<Cloud> for soft white clouds high above the island (y ≈ 40–80, spread out, slow drift). Change the <Environment preset="night"> to a daytime preset with background={false} so it only lights the scene. Keep the directional light aligned with the new sun position so shadows stay correct.
Task 2 — White space visible from far away: When the camera is far from the island, white/empty areas show. Fix with: an explicit background <color attach="background" args={["#87ceeb"]} />, ensure the Sky covers all camera distances, and set matching fog (<fog attach="fog" args={["#87ceeb", 60, 220]} />) so the horizon blends. Adjust the existing fog in GameEnvironment.tsx to match this color instead of stacking two fogs. Verify by zooming far out — no white at any distance.
Task 3 — Autumn trees GLB oversized and floating: The autumn trees model is far too large and its trunks/leaves hover above the ground. Find where it loads, scale it down until trees are roughly 3–5× the character's height, and lower its y-position so trunk bases sit exactly on the terrain. Report final scale/position values.
Task 4 — Solid objects (collision): The character walks through trees and objects. Without adding a physics engine and without rewriting the controller: create src/collision/colliders.ts exporting cylinder colliders {x, z, radius} for major trees, the temple, and other solids, plus a resolveCollision(position) helper that pushes an overlapping position out. Insert a single call to this helper at the point where the player's new position is applied each frame — one line only; if there's no safe single insertion point in the movement code, stop and report the exact file/line instead of improvising. Tune radii so the character stops at trunks without sticking.
Verify: blue sky + clouds, zero white at any zoom, autumn trees grounded and proportionate, character blocked by solids, HUD and shadows intact, no console errors. Report changes per task.